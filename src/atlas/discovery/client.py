"""HTTP client used by discovery connectors."""

from __future__ import annotations

from atlas.common.http import AtlasHttpClient


class DiscoveryHttpClient(AtlasHttpClient):
    """HTTP client for discovery connectors."""

    pass
